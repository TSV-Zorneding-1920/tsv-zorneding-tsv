{
  "name": "@tsv-zorneding/tsv",
  "version": "0.0.1",
  "author": "Christian Fritsch",
  "scripts": {
    "clean": "gatsby clean",
    "start:app": "npm run develop",
    "start": "run-p start:**",
    "build:app": "npm run clean && gatsby build",
    "build": "gatsby build",
    "develop": "npm run clean && gatsby develop",
    "serve": "gatsby serve",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write \"{gatsby-*.js,src/**/*.js}\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "gatsby": "^2.19.7",
    "gatsby-source-rss-feed": "^1.2.2",
    "gatsby-theme-tsv-zorneding-dependencies": "file:./plugins/gatsby-theme-tsv-zorneding/package",
    "node-sass": "^7.0.0",
    "parcel-bundler": "^1.12.3",
    "react": "^16.12.0",
    "react-dom": "^16.9.0",
    "uuid": "^3.2.1"
  }
}
